import kotlin.math.PI 
//Name:OYEKALE OLAMIDE DAVID 
// App to calculate the radius of a circle
fun main(){ print("Input the value of radius: ") 
val radius = readln().toDouble() 
val areaOfCircle = PI * radius * radius 
println("The area of the circle of radius $radius is $areaOfCircle") }
