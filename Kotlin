import kotlin.math.PI // App to calculate the radius of a circlefun main(){ print("Input the value of radius: ") val radius = readln().toDouble() val areaOfCircle = PI * radius * radius println("The area of the circle of radius $radius is $areaOfCircle") }
